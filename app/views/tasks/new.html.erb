<h1>新規タスク作成ページ</h1>

<%= form_for(@task) do |f| %>
  
  <%= render 'form', task: @task %>
  
  <%= f.label :content, 'タスク' %>  <%= f.text_field :content %>
  <%= f.label :status, '状態' %>  <%= f.text_field :status %>
  
  <%= f.submit '投稿' %>
<% end %>

  
<%= link_to 'タスク一覧に戻る', tasks_path %>


<h1>タスクNo. <%= @task.id %> の編集ページ</h1>

<%= form_for(@task) do |f| %>
<%= render 'form', task: @task %>

  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
 <%= f.label :status, '状態' %>
  <%= f.text_field :status %>
 
  <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る', tasks_path %>